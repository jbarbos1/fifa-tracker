# Core
Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0        # Alembic wrapper for `flask db` commands
psycopg2-binary>=2.9      # PostgreSQL driver (use psycopg2 in prod)

# Analysis
numpy>=1.26
pandas>=2.2
scipy>=1.13               # for IRR, stats distributions in Monte Carlo

# Dev
python-dotenv>=1.0        # load .env for DB URI etc.